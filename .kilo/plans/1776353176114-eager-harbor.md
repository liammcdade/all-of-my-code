# Plan: Fix Chess GUI and implement AI vs AI self-play mode

## Issues identified
1. **pygame initialization bug**: `pygame.init()` is never called, causing "font not initialized" error at line 694
2. **pkg_resources deprecation warning**: `pkg_resources` will be removed in setuptools 81+
3. **User request**: Modify GUI to have AI play both sides of the board (self-play mode)

## Implementation Steps

### 1. Fix pygame initialization
- Add `pygame.init()` at the very start of `ChessGUI.__init__()` method before any other pygame calls
- Also add `pygame.font.init()` explicitly to ensure font subsystem is properly initialized

### 2. Replace pkg_resources usage
- Replace any `resource_stream` and `resource_exists` imports with modern `importlib.resources` equivalents
- This is required for compatibility with setuptools >=81

### 3. Implement AI vs AI self-play mode
- Add a game mode toggle or automatic self-play functionality
- Modify the main game loop to:
  - After each valid move, automatically trigger the AI to make the next move
  - Remove human input handling when in self-play mode
  - Add a small delay between moves so the game is visible
  - Keep displaying the board state between moves
- Add status updates showing which AI color is thinking/moving
- Handle game over state properly with final result display

### 4. Optional enhancements
- Add speed control for self-play moves
- Add pause/resume functionality
- Add option to switch between human vs AI and AI vs AI modes
- Display move history or game statistics

## Files to modify
1. `sportsanalysis/chess/gui.py` - main GUI class, game loop, pygame initialization
2. Any other files importing pkg_resources
